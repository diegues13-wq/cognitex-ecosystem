# Build output
dist
dist-ssr

# Vite's dependency pre-bundle cache. This was committed to the repository —
# `.vite/deps/_metadata.json` and a stray package.json — so a machine-local
# cache travelled with every checkout.
.vite
node_modules

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Local overrides
*.local
.env
.env.*
!.env.example

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.sw?
